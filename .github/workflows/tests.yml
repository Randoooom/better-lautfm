on: [pull_request]
name: Tests

jobs:
  test:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - name: Install packages
        run: npm install
      - name: Build
        run: npm run build
      - name: Tests
        run: npm test